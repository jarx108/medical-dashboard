<UserControl x:Class="prgPMR.WPF.Modules.Immunization.ImmunizationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:prgPMR.WPF.Modules.Immunization">

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>  <!-- Title -->
            <RowDefinition Height="Auto"/>  <!-- Error banner -->
            <RowDefinition Height="*"/>     <!-- DataGrid -->
            <RowDefinition Height="Auto"/>  <!-- Action buttons -->
        </Grid.RowDefinitions>

        <!-- Title -->
        <TextBlock Grid.Row="0" Text="Immunizations"
                   FontSize="22" FontWeight="Bold"
                   Foreground="#2C3E50" Margin="0,0,0,12"/>

        <!-- Error banner -->
        <Border Grid.Row="1" Background="#FDECEA" CornerRadius="4" Padding="10,6"
                Margin="0,0,0,8"
                Visibility="{Binding HasError, Converter={StaticResource BoolToVis}}">
            <TextBlock Text="{Binding ErrorMessage}" Foreground="#C0392B"/>
        </Border>

        <!-- DataGrid -->
        <DataGrid Grid.Row="2"
                  ItemsSource="{Binding Items}"
                  SelectedItem="{Binding SelectedItem}"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  SelectionMode="Single"
                  GridLinesVisibility="Horizontal"
                  HeadersVisibility="Column"
                  AlternatingRowBackground="#F8F9FA"
                  BorderBrush="#DEE2E6"
                  BorderThickness="1"
                  Margin="0,0,0,12">

            <DataGrid.Columns>
                <DataGridTextColumn Header="Vaccine Name"  Binding="{Binding VaccineName}"  Width="2*"/>
                <DataGridTextColumn Header="Group"         Binding="{Binding VaccineGroup}"  Width="*"/>
                <DataGridTextColumn Header="Total Doses"   Binding="{Binding TotalDoses}"    Width="80" ElementStyle="{StaticResource {x:Type TextBlock}}"/>
                <DataGridTextColumn Header="Last Dose"     Binding="{Binding DateLastDose, Converter={StaticResource DateConv}}" Width="100"/>
                <DataGridTextColumn Header="Dose 1"        Binding="{Binding Dose1, Converter={StaticResource DateConv}}" Width="100"/>
                <DataGridTextColumn Header="Dose 2"        Binding="{Binding Dose2, Converter={StaticResource DateConv}}" Width="100"/>
                <DataGridTextColumn Header="Dose 3"        Binding="{Binding Dose3, Converter={StaticResource DateConv}}" Width="100"/>
                <DataGridTextColumn Header="Dose 4"        Binding="{Binding Dose4, Converter={StaticResource DateConv}}" Width="100"/>
                <DataGridTextColumn Header="Dose 5"        Binding="{Binding Dose5, Converter={StaticResource DateConv}}" Width="100"/>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Action buttons — visibility and enabled driven by ViewModel properties directly -->
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Add"
                    Command="{Binding AddCommand}"
                    Style="{StaticResource ActionButton}"/>
            <Button Content="Edit"
                    Command="{Binding EditCommand}"
                    Style="{StaticResource ActionButton}"
                    Visibility="{Binding HasSelection, Converter={StaticResource BoolToVis}}"/>
            <Button Content="Delete"
                    Command="{Binding DeleteCommand}"
                    Style="{StaticResource DangerButton}"
                    Visibility="{Binding HasSelection, Converter={StaticResource BoolToVis}}"/>
            <Button Content="Reset"
                    Command="{Binding ResetCommand}"
                    Style="{StaticResource ActionButton}"
                    Background="#95A5A6"/>
        </StackPanel>

        <!-- Loading overlay -->
        <Grid Grid.RowSpan="4" Background="#80FFFFFF"
              Visibility="{Binding IsBusy, Converter={StaticResource BoolToVis}}">
            <TextBlock Text="Loading…" VerticalAlignment="Center"
                       HorizontalAlignment="Center" FontSize="16"/>
        </Grid>
    </Grid>
</UserControl>
