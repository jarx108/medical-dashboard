<UserControl x:Class="prgPMR.WPF.Modules.Medications.MedicationsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Medications"
                   FontSize="22" FontWeight="Bold" Foreground="#2C3E50" Margin="0,0,0,12"/>

        <Border Grid.Row="1" Background="#FDECEA" CornerRadius="4" Padding="10,6"
                Margin="0,0,0,8"
                Visibility="{Binding HasError, Converter={StaticResource BoolToVis}}">
            <TextBlock Text="{Binding ErrorMessage}" Foreground="#C0392B"/>
        </Border>

        <DataGrid Grid.Row="2"
                  ItemsSource="{Binding Items}"
                  SelectedItem="{Binding SelectedItem}"
                  AutoGenerateColumns="True"
                  IsReadOnly="True"
                  SelectionMode="Single"
                  GridLinesVisibility="Horizontal"
                  AlternatingRowBackground="#F8F9FA"
                  BorderBrush="#DEE2E6" BorderThickness="1"
                  Margin="0,0,0,12"/>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Add"    Command="{Binding AddCommand}"    Style="{StaticResource ActionButton}"/>
            <Button Content="Edit"   Command="{Binding EditCommand}"   Style="{StaticResource ActionButton}"
                    Visibility="{Binding HasSelection, Converter={StaticResource BoolToVis}}"/>
            <Button Content="Delete" Command="{Binding DeleteCommand}" Style="{StaticResource DangerButton}"
                    Visibility="{Binding HasSelection, Converter={StaticResource BoolToVis}}"/>
            <Button Content="Reset"  Command="{Binding ResetCommand}"  Style="{StaticResource ActionButton}" Background="#95A5A6"/>
        </StackPanel>

        <Grid Grid.RowSpan="4" Background="#80FFFFFF"
              Visibility="{Binding IsBusy, Converter={StaticResource BoolToVis}}">
            <TextBlock Text="Loading..." VerticalAlignment="Center" HorizontalAlignment="Center" FontSize="16"/>
        </Grid>
    </Grid>
</UserControl>
